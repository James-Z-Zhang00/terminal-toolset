# terminal-toolset

My personal developed terminal tools for Bash/Zsh

### List
1. `zsh_alias.sh` - some command shortcuts, including basic directory change, opening apps and folders
2. `folder_descrition.sh` - allows to write a description for rarely used folders, the description will display in terminal when switching to the directory. The description can be removed by `rm .folder_description`